Try mapping over the result of `product`.

def product[A,B](p: Parser[A], p2: Parser[B]): Parser[(A,B)]

def map2[A,B,C](p: Parser[A], p2: Parser[B])(f: (A, B)=>C) = {
    map(product(p, p2))( t.fupled )
}